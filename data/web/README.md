# Web UI — placeholder

Web UI static files go here (index.html, app.js, style.css, ...).

Uploaded to the `littlefs_sys` partition via:
  pio run -e uploadfs-sys -t uploadfs

This folder is intentionally empty until the web UI is implemented (future wave).
